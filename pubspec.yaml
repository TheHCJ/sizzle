name: sizzle
description: A social feed reader, that doesn't suck ass
publish_to: 'none'
version: 0.0.0-indev

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
